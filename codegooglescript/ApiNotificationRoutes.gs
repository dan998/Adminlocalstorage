// ===================================================== // APINOTIFICATIONROUTES.GS // Notification API Routes // Registration System API // ===================================================== // ===================================================== // NOTIFICATION ROUTER // ===================================================== function handleNotificationRoutes( action, data ) { data = data || {}; // =================================================== // CREATE NOTIFICATION // =================================================== if ( action === "createNotification" ) { return createNotification( data ); } // =================================================== // GET NOTIFICATIONS // =================================================== if ( action === "getNotifications" ) { return getNotifications( data ); } // =================================================== // GET SINGLE NOTIFICATION // =================================================== if ( action === "getNotification" ) { return getNotification( data ); } // =================================================== // GET UNREAD NOTIFICATIONS // =================================================== if ( action === "getUnreadNotifications" ) { return getUnreadNotifications( data ); } // =================================================== // GET UNREAD COUNT // =================================================== if ( action === "getUnreadCount" ) { return getUnreadCount( data ); } // =================================================== // MARK NOTIFICATION AS READ // =================================================== if ( action === "markRead" ) { return markRead( data ); } // =================================================== // MARK ALL AS READ // =================================================== if ( action === "markAllRead" ) { return markAllRead( data ); } // =================================================== // MARK NOTIFICATION AS UNREAD // =================================================== if ( action === "markUnread" ) { return markUnread( data ); } // =================================================== // DELETE NOTIFICATION // =================================================== if ( action === "deleteNotification" ) { return deleteNotification( data ); } // =================================================== // DELETE ALL NOTIFICATIONS // =================================================== if ( action === "deleteAllNotifications" ) { return deleteAllNotifications( data ); } // =================================================== // CLEAR READ NOTIFICATIONS // =================================================== if ( action === "clearReadNotifications" ) { return clearReadNotifications( data ); } // =================================================== // UPDATE NOTIFICATION // =================================================== if ( action === "updateNotification" ) { return updateNotification( data ); } // =================================================== // NOTIFICATION COUNT // =================================================== if ( action === "notificationCount" ) { return notificationCount( data ); } // =================================================== // NO NOTIFICATION ROUTE // =================================================== return null; } 